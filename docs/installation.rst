.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install base_model

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv base_model
    $ pip install base_model
